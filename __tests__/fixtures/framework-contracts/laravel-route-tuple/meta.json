{
  "resolver": "laravel",
  "sourcePath": "routes/web.php",
  "description": "Laravel `Route::get('/users', [UserController::class, 'index'])` — produces one route node referencing the controller action."
}
